spring:
  cloud:
    vault:
      host: <%= p('vault.host')%>
      port: <%= p('vault.port')%>
      scheme: <%= p('vault.scheme')%>
      authentication: <%= p('vault.authentication')%>
      token: <%= p('vault.token')%>
